# Render.com deployment configuration
# https://render.com/docs/blueprint-spec

services:
  # Django Backend API
  - type: web
    name: fundi-api
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate --noinput
    startCommand: gunicorn fundi.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: DJANGO_DEBUG
        value: "false"
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_ALLOWED_HOSTS
        value: ".onrender.com"
      - key: CORS_ALLOWED_ORIGINS
        sync: false # Set this in Render dashboard to your Vercel URL
      - key: USE_SQLITE
        value: "true" # Use SQLite for demo, switch to false for PostgreSQL
      - key: SECURE_SSL_REDIRECT
        value: "true"
      - key: PYTHON_VERSION
        value: "3.11.4"

# Optional: Add PostgreSQL database
# databases:
#   - name: fundi-db
#     plan: free
#     databaseName: fundi
#     user: fundi_user
