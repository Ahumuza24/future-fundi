---
trigger: always_on
---

Use DRF throttling and rate limiting.
Use DRF pagination on all list views.
Keep payloads minimal.
Avoid returning large nested trees of data.
Use caching with Django cache backend where appropriate.
Implement endpoint level validation and permission checks early.